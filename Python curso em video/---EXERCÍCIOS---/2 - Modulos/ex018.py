import math
ang = float(input('digite um angulo qualquer: '))
print('o seno de {} é {}'.format(ang, math.sin(math.radians(ang))))
print('o cosseno de {} é {}'.format(ang, math.cos(math.radians(ang))))
print('a tangente de {} é {}'.format(ang, math.tan(math.radians(ang))))
